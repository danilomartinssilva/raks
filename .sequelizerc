const { resolve } = require('path');
 
module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'modules','raks', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'shared','infra', 'sequelize','migrations'),
  'seeders-path': resolve(__dirname, 'src', 'shared','infra', 'sequelize','seeds'),
}